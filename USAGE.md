# Usage

1) Install the environment (Conda or pip) — see README.
2) Put `adnidata.csv` in `data/` (Demo Version).
3) Open `notebooks/Final_Version.ipynb` in Jupyter.
4) Run all cells to reproduce preprocessing, model selection, and evaluation.
